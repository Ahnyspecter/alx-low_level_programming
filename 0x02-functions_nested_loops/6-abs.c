#include "main.h"

/**
 * _abs - computes the absolute of an integer
 * @n: the int to check
 * Return: the absolute value if int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}
